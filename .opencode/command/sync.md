---
description: Sync the forge fork with upstream stablyai/orca (fetch, merge, resolve whitelist conflicts, run gates, report).
---
Load and follow the `forge-sync` skill (`.opencode/skills/forge-sync/SKILL.md`), then perform a full sync now.

$ARGUMENTS
